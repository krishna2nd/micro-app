import get from "lodash";

export const getSkuDetails = ({ PdpReducer: {product} }) => {
  // console.log('home_categories',home_categories)
  // const carousels = desktop.filter(
  //   component => component.name === "ProductsCarousel"
  // );

  return product;
};
